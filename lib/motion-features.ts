// The animation features framer-motion needs on this site (no drag, no layout
// projection). Loaded as its own chunk after first paint via LazyMotion.
export { domAnimation as default } from "framer-motion";
